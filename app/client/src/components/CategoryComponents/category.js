const Category = (pageTitle) => {
  return (
    <div className="centered-text category-page-head">
      <h3>{`Discover the most popular free ${pageTitle}`} </h3>
    </div>
  );
};

export default Category;
